{
  "name": "stylelint-css-in-js-demo",
  "version": "1.0.0",
  "description": "Demo of Stylelint with CSS-in-JS (styled-components)",
  "private": true,
  "scripts": {
    "lint": "stylelint '**/*.{js,jsx}'",
    "lint:fix": "stylelint '**/*.{js,jsx}' --fix"
  },
  "devDependencies": {
    "postcss-styled-syntax": "^0.7.0",
    "stylelint": "^16.24.0",
    "stylelint-config-standard": "^36.0.1"
  }
}